#My First Workflow

name: workflow1
on:
  push:
  workflow_dispatch:
  
  
jobs:
  job1:
    runs-on: ubuntu-20.04
    steps:
      - name: step1
        run: |
          echo "Hello from CF"
          sleep 30
      - name: step2 for testing
        run: |
          cat /etc/os-release 
          sleep 30
    ## Name us not important in a step
      - run: uname -a

      
  
